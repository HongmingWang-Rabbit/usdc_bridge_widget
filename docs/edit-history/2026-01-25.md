# Edit History - January 25, 2026

## Summary

Code review and fixes session. Fixed all issues identified in comprehensive code review including critical event listener cleanup issues, race condition handling, and theme improvements.

## Code Review Findings

### Initial Review (17 issues)
- 2 Critical issues
- 7 Warnings
- 8 Suggestions

### Fixes Applied

#### Critical Fixes

1. **Event listener cleanup in useBridge.ts**
   - Problem: Event listeners in catch block used empty arrow functions instead of actual handlers
   - Solution: Refactored to store cleanup function in variable accessible to both try and catch blocks

2. **Race condition in approval flow (BridgeWidget.tsx)**
   - Problem: Inputs remained enabled during pending operations
   - Solution: Added `isOperationPending` state and `disabled` props to ChainSelector, SwapButton, AmountInput

#### Warning Fixes

3. **Floating promise in useEffect (BridgeWidget.tsx:875)**
   - Added `void` prefix to handle floating promise correctly

4. **Deprecation warnings (hooks.ts)**
   - Simplified to always fire (browser-compatible, removed Node.js process check)

5. **Hardcoded hover color (BridgeWidget.tsx:401)**
   - Added `hoverColor` to theme system
   - Updated light theme preset with appropriate hover color
   - BridgeWidget now uses `theme.hoverColor`

#### Suggestion Fixes

6. **Chain mapping documentation (useBridge.ts:13-31)**
   - Added comment noting mapping must be kept in sync with Circle SDK updates

## Files Modified

### Source Files
- `src/useBridge.ts` - Event listener cleanup, chain mapping documentation
- `src/BridgeWidget.tsx` - Floating promise fix, theme.hoverColor usage
- `src/hooks.ts` - Simplified deprecation warnings
- `src/types.ts` - Added hoverColor to BridgeWidgetTheme interface
- `src/theme.ts` - Added hoverColor to defaultTheme and light preset

### Test Files
- `src/__tests__/theme.test.ts` - Added tests for hoverColor

### Documentation
- `CLAUDE.md` - Updated file structure, added theme system docs, deprecation notes
- `README.md` - Added hoverColor to theme docs, updated hooks section, fixed Monad chain ID

## Build Output
- CJS: 73.79 KB
- ESM: 67.04 KB
- Types: 29.65 KB

## Test Results
- 153 tests passing across 8 test files
